version: "3"

services:
  app:
    image: your_app_image
    deploy:
      replicas: 4

  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - app
